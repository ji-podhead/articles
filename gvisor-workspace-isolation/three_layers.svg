<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1050 560" width="100%" height="100%">
  <defs>
    <filter id="sh" x="-5%" y="-5%" width="110%" height="110%">
      <feDropShadow dx="0" dy="2" stdDeviation="3" flood-opacity="0.1"/>
    </filter>
    <marker id="arr" viewBox="0 0 10 10" refX="6" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
      <path d="M 0 1 L 10 5 L 0 9 z" fill="#94a3b8"/>
    </marker>
  </defs>
  <rect width="1050" height="560" fill="#f8fafc" rx="12"/>
  <text x="525" y="30" fill="#334155" font-family="sans-serif" font-weight="bold" font-size="16" text-anchor="middle">One request, three independent gates — and what each one catches if the others fail</text>

  <rect x="60" y="55" width="200" height="50" rx="8" fill="#ffffff" stroke="#cbd5e1" filter="url(#sh)"/>
  <text x="160" y="85" fill="#334155" font-family="sans-serif" font-weight="bold" font-size="12" text-anchor="middle">Client request</text>
  <line x1="160" y1="105" x2="160" y2="130" stroke="#94a3b8" stroke-width="2" marker-end="url(#arr)"/>

  <!-- Layer C: Identity Proxy -->
  <rect x="30" y="135" width="990" height="105" rx="10" fill="#fff7ed" stroke="#fdba74"/>
  <text x="55" y="160" fill="#9a3412" font-family="sans-serif" font-weight="bold" font-size="13">LAYER C — Identity Proxy (the bouncer)</text>
  <text x="55" y="180" fill="#7c2d12" font-family="sans-serif" font-size="10">Terminates TLS · verifies the session/JWT · extracts app + owner from the URL · adds X-App-ID · no raw port is ever exposed</text>
  <text x="55" y="200" fill="#c2410c" font-family="sans-serif" font-weight="bold" font-size="10">What it does NOT catch: anything that happens AFTER a legitimate, authenticated user is already inside their own workspace.</text>
  <text x="55" y="218" fill="#7c2d12" font-family="sans-serif" font-size="9">If this were the only layer: a legitimate user runs malicious code inside their own container — the proxy only watches the front door, never sees this "east-west" traffic.</text>

  <line x1="160" y1="240" x2="160" y2="265" stroke="#94a3b8" stroke-width="2" marker-end="url(#arr)"/>

  <!-- Layer B: VLAN -->
  <rect x="30" y="270" width="990" height="105" rx="10" fill="#f5f3ff" stroke="#c4b5fd"/>
  <text x="55" y="295" fill="#4c1d95" font-family="sans-serif" font-weight="bold" font-size="13">LAYER B — VLAN / ipvlan (the soundproof wall)</text>
  <text x="55" y="315" fill="#3730a3" font-family="sans-serif" font-size="10">Every tenant's traffic is 802.1Q-tagged (vlan.id) on a dedicated sub-interface · the kernel makes cross-tenant packets physically unroutable</text>
  <text x="55" y="335" fill="#6d28d9" font-family="sans-serif" font-weight="bold" font-size="10">What it does NOT catch: a kernel escape on the host itself.</text>
  <text x="55" y="353" fill="#3730a3" font-family="sans-serif" font-size="9">If this were the only layer: a zero-day in the real Linux kernel lets an attacker break out of runc entirely — from there, one `ip link` command on the host undoes every VLAN you configured.</text>

  <line x1="160" y1="375" x2="160" y2="400" stroke="#94a3b8" stroke-width="2" marker-end="url(#arr)"/>

  <!-- Layer A: gVisor -->
  <rect x="30" y="405" width="990" height="105" rx="10" fill="#ecfdf5" stroke="#6ee7b7"/>
  <text x="55" y="430" fill="#065f46" font-family="sans-serif" font-weight="bold" font-size="13">LAYER A — gVisor (the straitjacket)</text>
  <text x="55" y="450" fill="#047857" font-family="sans-serif" font-size="10">Application syscalls never reach the real host kernel — the Sentry emulates them in a Go userspace process (see the architecture diagram above)</text>
  <text x="55" y="470" fill="#059669" font-family="sans-serif" font-weight="bold" font-size="10">What it does NOT catch: an attacker already inside the sandbox scanning the LOCAL network.</text>
  <text x="55" y="488" fill="#047857" font-family="sans-serif" font-size="9">If this were the only layer: gVisor hands normal Ethernet frames to the host bridge — nothing stops the sandboxed app from nmap-scanning a neighboring tenant's container on the same unsegmented bridge.</text>

  <rect x="230" y="518" width="590" height="34" rx="6" fill="#1e293b"/>
  <text x="525" y="540" fill="#38bdf8" font-family="sans-serif" font-weight="bold" font-size="11" text-anchor="middle">Each layer's blind spot is exactly the next layer's job — that's why all three run together, not any one alone.</text>
</svg>
